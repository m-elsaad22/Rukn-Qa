# إصلاحات حية — ركن التطور قطر

**الموقع:** https://www.rukn-eltatawer.com/qa  
**التاريخ:** 16 سبتمبر 2026  
**سياسة التواصل المطلوبة:** واتساب الإمارات `+971 58 663 4710` يبقى ظاهراً. أزرار الاتصال مخفية. لا رقم قطري حتى يُوفَّر رقم جديد.

لا تُنشر مسودات المدينة×الخدمة الـ1662 كما هي. لا تُحفظ كلمات مرور التطبيقات في المستودع.

## ما طُبّق على ووردبريس

| الطبقة | التغيير |
| --- | --- |
| Code Snippet 5 | الإبقاء على `RUKN_QA_WA=971586634710` و`HIDE_CALL=true`، مسح أرقام 974، حذف ترويسة KAYAN WEB، `tel` → غير قابل للضغط، جملة `974+` في الأسئلة |
| Code Snippet 6 (جديدة، مفعّلة) | إحصائيات وplaceholders، مدن الباحث، 404 حقيقية، أرشيف CPT، Polylang `browser=0`، Schema/hreflang، إخفاء النموذج الميت والتقييم الوهمي |
| خيارات | `whatsapp_number=971586634710`، `phonenumber` فارغ، `rukn_hide_call_global=1` |
| صفحات | من نحن، اتصل بنا، الخدمات، الأسعار، الأسئلة، المدن، Contact EN، المدونة |
| مقالات EN | إعادة كتابة 12866/12867/12868 بدون شورت كود أو فقرات عربية |
| CPT خدمات | عازل الصوت أصبح عزل صوت لا ماء/حرارة؛ كشف التسربات يشير للمقال الطويل |
| إلغاء نشر | استقدام، تخزين/سكراب/مستعمل، ونسخ مكررة (تنظيف/صراصير/مجاري الدوحة/غاز الدوحة) مع تحويل Rank Math |
| قوائم | عناوين قصيرة؛ قائمة EN أُضيف لها المقالات الثلاثة |

## تحقق حي بعد التطهير

| المسار | النتيجة |
| --- | --- |
| `/qa/` | واتساب 971، لا 974، لا `tel:`، لا KAYAN WEB، عدّاد 20 خدمة / 9 مدن، الشمال والظعاين في الباحث |
| `/qa/en/` | لا يبقى على ترجمة المتصفح؛ لا `Questions asked` ولا `[[projects]]` |
| `/qa/services/` | صفحة الخدمات العربية لا أرشيف `services` |
| `/qa/services/sound-insulation-installation/` | 200، H1 عازل الصوت، نص مسار الصوت |
| `/qa/services/landscaping/` | 200 |
| مسار وهمي | 404 مع رسالة «الصفحة غير موجودة» لا 302 للرئيسية |
| `/qa/recruitment-in-qatar/` | 301 إلى الرئيسية |

## ما بقي عن قصد أو خارج نطاق هذه الدفعة

- رقم قطري: يُضاف لاحقاً في القصاصة (`RUKN_QA_PHONE` / `RUKN_QA_WA`) ثم يُظهر زر الاتصال إن طُلب.
- المقالات الرفيعة الـ135 ذات القالب المكرر: لم تُعاد كتابتها كلها؛ أُخرج خارج التخصص والمكرر فقط.
- ودجات Kayan seed (15 ألف عميل…) ليست واجهة الهوم الحالية؛ نُظفت احتياطياً.
- بقايا CSS باسم YourColor وملف الشعار `qa-logo-foter` تحتاج تعديلاً في ملفات القالب على السيرفر.
- نموذج اتصل بنا في ودجت القالب ما زال `action="#"` لذلك أُخفي؛ التواصل عبر واتساب.

## دفعة 23 سبتمبر 2026 — فحص بقايا الإمارات في القالب

التفاصيل في [template-uae-audit-2026-09.md](template-uae-audit-2026-09.md). القصاصة 5 لم تُمس. واتساب 971 بقي. أزرار الاتصال بقيت مخفية.

| الطبقة | التغيير |
| --- | --- |
| Code Snippet 6 → v9 | CSS الأعلام لم يعد يستخدم كلمة `الإمارات` حتى لا تُحوَّل إلى `قطر` وتُخفى أيقونات قطر. إخفاء أزرار دول الخليج. استبدال عجمان فقط (لا عمان). 8→9 مدن. FAQ الشمال/الظعاين. توجيه بطاقات التكييف/السباكة في لوحة الهوم. `[[عدد Projects]]` |
| Code Snippet 18 | أُوقفت. كانت تكتب ملفات kayan-theme في كل طلب |

بعد الإيقاف الهيدر الحي يعرض مبدّل لغة AR/EN فقط بلا قائمة ae/sa/kw/om/bh/eg.

## دفعة 23 سبتمبر 2026 (لاحقاً) — تطبيق شكل Call-main وإغلاق بقايا القالب

الأرشيف `content-audit/designs/Call-main.zip` فُكّ واستُخدم كمرجع شكل فقط. النصوص الإماراتية (دبي، 15 ألف عميل، ضمان 10 سنوات، اتصال هاتفي) لم تُنسخ. القصاصة 5 لم تُمس. واتساب 971 بقي. أزرار الاتصال بقيت مخفية.

| الطبقة | التغيير |
| --- | --- |
| Code Snippet 6 → v10 | حقن قائمة الهيدر والجوال من شكل التصميم؛ وصف فوتر بلا إيموجي؛ شعار `qa-logo.webp` بدل `qa-logo-foter`؛ حذف روابط X/LinkedIn/Instagram الشخصية؛ صفحة 404 بـ `err-wrap`؛ ربط عناصر الدليل بدل `href="#"`؛ حذف نماذج `action="#"` من HTML؛ تعبئة DNI واتساب 971 مع إبقاء الهاتف فارغاً |
| صفحات WP | من نحن (7460) بطاقات مهمة/طريقة عمل + مسار رباعي؛ اتصل بنا (7461) بلا فورم ميت؛ الأسئلة (7467) أكورديون `faq-item` |

تحقق بعد التطبيق: القائمة العربية في `/qa/`، بطاقات من نحن، 404 `err-wrap`، DNI `wa_number=971586634710`. أول رفع لـ v10 كان يُفرّغ الصفحات لأن `preg_replace` استخدم `#` مع `action="#"` فأعاد `null`؛ أُصلح فوراً.
